const HeroesNotFoundMessage = () => {
    return (
        <div className="flex-grow flex items-center justify-center">
            <h2 className="text-5xl">No heroes found!</h2>
        </div>
    );
};

export default HeroesNotFoundMessage;
